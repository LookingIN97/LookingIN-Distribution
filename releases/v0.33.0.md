# LookingIN 0.33.0

- Added a signed, independently running Windows updater. It verifies the
  manifest, package size/hash and strict ZIP file list before waiting for the
  game and transactionally replacing the plugin installation.
- The plugin starts the updater best-effort and shows `| restart to update`
  beside the LKI version only after a verified package is ready.

Build: `2721a33ad64e`
