# LookingIN 0.33.2

- Accepted and correctly skipped the formal ZIP's single `LookingIN/` root
  directory entry during validated extraction, and stopped emitting that
  directory entry in new packages for compatibility with the first updater.

Build: `e2b229e3076c`
